import React from 'react'

import { Container } from './styles'
import Button from '@/components/blocks/Button'

const Form = props => {
  return (
    <Container>
      <textarea></textarea>
      <Button>Add</Button>
    </Container>
  )
}

export default Form
